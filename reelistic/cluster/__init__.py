"""Cluster-only data preparation, training, evaluation, and audit entry points.

The filenames in this package are stable because Slurm scripts invoke them
directly. Shared model and metric logic belongs in :mod:`aigc_detector`.
"""
